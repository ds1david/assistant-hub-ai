<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>ai.assistanthub</groupId>
    <artifactId>assistant-hub-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <relativePath>../../pom.xml</relativePath>
  </parent>
  <artifactId>session-core</artifactId>

  <dependencies>
    <dependency>
      <groupId>ai.assistanthub</groupId>
      <artifactId>plugin-sdk-java</artifactId>
      <version>${project.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-websocket</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.networknt</groupId>
      <artifactId>json-schema-validator</artifactId>
      <version>1.5.0</version>
    </dependency>
    <dependency>
      <groupId>org.xerial</groupId>
      <artifactId>sqlite-jdbc</artifactId>
      <version>3.46.1.3</version>
    </dependency>
  </dependencies>

  <build>
    <resources>
      <resource>
        <directory>src/main/resources</directory>
      </resource>
      <resource>
        <!-- Copia o contrato autoritativo (P4 — fonte única em contracts/ na raiz do monorepo)
             para o classpath, sem manter uma segunda cópia versionada. -->
        <directory>${project.basedir}/../../contracts</directory>
        <targetPath>contracts</targetPath>
        <includes>
          <include>transcript-event.v2.schema.json</include>
          <include>ai-provider-profile.v1.schema.json</include>
        </includes>
      </resource>
    </resources>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <!-- version from parent pluginManagement -->
        <configuration>
          <!-- Override parent skip=true so this is the only module that runs. -->
          <skip>false</skip>
          <!--
            Relative paths in application.yml (config/ai-providers.yaml,
            data/session-core/) are monorepo-root-relative. spring-boot:run
            defaults the forked JVM CWD to this module; pin it to the reactor root.
          -->
          <workingDirectory>${maven.multiModuleProjectDirectory}</workingDirectory>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
