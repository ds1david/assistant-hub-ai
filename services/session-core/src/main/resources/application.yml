server:
  port: 8080
spring:
  application:
    name: assistant-hub-session-core
management:
  endpoints:
    web:
      exposure:
        include: health,info
session-core:
  transcript-ingestion:
    feed-url: ${SESSION_CORE_TRANSCRIPT_FEED_URL:ws://localhost:8001/ws/transcripts}
